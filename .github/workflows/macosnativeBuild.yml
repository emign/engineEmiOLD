name: macosnativebuild

on: push

jobs:
  gradle:
    strategy:
      matrix:
        os: [macos-latest]
    runs-on: [macos-latest]
    steps:
      - uses: actions/checkout@v1
      - uses: eskatos/gradle-command-action@v1
        with:
          arguments: compileKotlinMacosX64